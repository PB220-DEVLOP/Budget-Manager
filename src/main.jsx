import ReactDOM from "react-dom/client";
import App from "./App.jsx";
// import "./index.css"; // Ensure index.css is in the src folder

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);